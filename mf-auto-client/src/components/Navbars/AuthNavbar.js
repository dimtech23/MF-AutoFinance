// Remove unused React import since we're using JSX
// import React from 'react';

// Empty AuthNavbar component that renders nothing
const AuthNavbar = () => {
  // Return null to render nothing
  return null;
};

export default AuthNavbar;